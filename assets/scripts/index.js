/*

    index.js is gonna handle the DOM and when the user presses the plus(add a task) and delete button
    - When pressed create an object from the constructor to create a task to be displayed
    - Will also be added to an array that will be
 */

//Date object
let d = new Date();

//grabs array from createTask and performs DOM to display array
class index {

}